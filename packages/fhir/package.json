{
  "name": "@clinic-os/fhir",
  "version": "0.0.0",
  "private": true,
  "description": "FHIR R4 projection and validation helpers.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "find src test -name '*.ts' -exec node --check {} \\;",
    "test": "node --test test/*.test.ts",
    "build": "tsc --noEmit -p tsconfig.json"
  }
}
