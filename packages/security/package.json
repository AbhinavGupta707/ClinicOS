{
  "name": "@clinic-os/security",
  "version": "0.0.0",
  "private": true,
  "description": "Audit, encryption, PHI redaction, and security policy helpers.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "node --check src/index.ts && node --check src/audit.ts && node --check src/redaction.ts",
    "test": "node --test test/*.test.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
