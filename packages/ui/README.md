# UI Package

Shared UI primitives and design system components for operational ClinicOS screens.

Checkpoint 1 exports minimal React primitives used by the web shell. Components are intentionally small until the product surfaces stabilize.
