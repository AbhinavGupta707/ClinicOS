{
  "name": "clinic-os",
  "version": "0.0.0",
  "private": true,
  "description": "ClinicOS India monorepo baseline.",
  "packageManager": "npm@10.9.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "node scripts/check-workspace.mjs && npm run check:clock && node --test tests/acceptance/cp11-typecheck-gate.test.mjs && npm run check:env && npm run format:check",
    "check:clock": "node scripts/check-clock-usage.mjs",
    "check:env": "npm --workspace @clinic-os/config run build && node scripts/validate-env-template.mjs",
    "ci": "npm run check && npm run typecheck && npm run lint && npm run test && npm run build && npm run security:audit && npm run security:secrets",
    "build:shared": "npm run build --workspace @clinic-os/domain --if-present && npm run build --workspace @clinic-os/db --if-present && npm run build --workspace @clinic-os/config --if-present && npm run build --workspace @clinic-os/observability --if-present && npm run build --workspace @clinic-os/integrations --if-present && npm run build --workspace @clinic-os/workflow --if-present",
    "typecheck": "npm run build:shared && npm run typecheck --workspaces --if-present",
    "lint": "npm run build:shared && npm run lint --workspaces --if-present",
    "test": "npm run build:shared && npm run test --workspaces --if-present",
    "build": "npm run build:shared && npm run build --workspaces --if-present",
    "format": "prettier --ignore-unknown --write package.json \"apps/*/package.json\" \"apps/*/README.md\" \"packages/config/**/*.{json,ts,md}\" \"scripts/**/*.{js,mjs}\" \".github/**/*.yml\" docker-compose.yml infra/README.md \"infra/docker/**/*.{md,json,yml,yaml,sql}\" \"infra/runbooks/**/*.{md,json,yml,yaml,sql}\" tsconfig.json tsconfig.base.json \"*.config.{js,mjs,ts}\"",
    "format:check": "prettier --ignore-unknown --check package.json \"apps/*/package.json\" \"apps/*/README.md\" \"packages/config/**/*.{json,ts,md}\" \"scripts/**/*.{js,mjs}\" \".github/**/*.yml\" docker-compose.yml infra/README.md \"infra/docker/**/*.{md,json,yml,yaml,sql}\" \"infra/runbooks/**/*.{md,json,yml,yaml,sql}\" tsconfig.json tsconfig.base.json \"*.config.{js,mjs,ts}\"",
    "security:audit": "npm audit --audit-level=high",
    "security:secrets": "node scripts/secret-scan.mjs",
    "local:services": "docker compose up -d postgres redis temporal temporal-ui keycloak",
    "local:up": "npm run local:services && npm run db:provision:local && npm run db:migrate && npm run db:grant-runtime",
    "local:down": "docker compose down --remove-orphans",
    "local:logs": "docker compose logs -f postgres redis temporal temporal-ui keycloak",
    "local:ps": "docker compose ps",
    "db:provision:local": "node scripts/db-local-lifecycle.mjs provision",
    "db:migrate": "docker compose --profile tools run --rm flyway migrate",
    "db:migrate:info": "docker compose --profile tools run --rm flyway info",
    "db:migrate:validate": "docker compose --profile tools run --rm flyway validate",
    "db:grant-runtime": "node scripts/db-local-lifecycle.mjs grant-runtime",
    "db:seed:local": "CLINIC_OS_ENV=local PILOT_SYNTHETIC_DATA_ONLY=true node scripts/db-local-lifecycle.mjs seed-local",
    "db:reset:local": "CLINIC_OS_ENV=local PILOT_SYNTHETIC_DATA_ONLY=true CLINIC_OS_ALLOW_LOCAL_DB_RESET=true node scripts/db-local-lifecycle.mjs reset-local",
    "db:verify": "node scripts/db-local-lifecycle.mjs verify",
    "db:test:migrations": "node scripts/test-db-migrations.mjs",
    "db:test:repositories": "node scripts/test-db-repositories.mjs",
    "worker:test:persistence": "npm --workspace @clinic-os/worker run build && node scripts/test-worker-persistence.mjs",
    "api:test:readiness": "node scripts/test-api-readiness.mjs",
    "cp11:smoke:api": "node scripts/cp10-contract-smoke.mjs",
    "cp11:baseline:api": "node scripts/cp11-performance-baseline.mjs",
    "db:bootstrap": "npm run local:services && npm run db:reset:local && npm run db:migrate && npm run db:grant-runtime && npm run db:seed:local && npm run db:verify",
    "dev:web": "npm --workspace @clinic-os/web run dev",
    "dev:api": "npm --workspace @clinic-os/api run dev",
    "dev:worker": "npm --workspace @clinic-os/worker run dev",
    "dev:mobile": "npm --workspace @clinic-os/mobile run dev"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "prettier": "^3.9.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vitest": "^4.1.10"
  }
}
