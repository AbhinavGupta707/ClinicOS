# Web App

Owner: platform and clinic product workstreams.

Checkpoint 1 will install and wire the production Next.js shell, auth session flow, role-aware navigation, and local dev command.

Reserved root command: `npm run dev:web`.
