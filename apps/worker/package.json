{
  "name": "@clinic-os/worker",
  "version": "0.0.0",
  "private": true,
  "description": "ClinicOS worker runtime. Checkpoint 1 will add outbox and Temporal workers.",
  "scripts": {
    "dev": "node ../../scripts/workspace-command-unavailable.mjs worker \"outbox and Temporal worker runtime\" \"Install the Temporal/outbox worker and register a real dev server in the Runtime/Workflow lane.\""
  }
}
