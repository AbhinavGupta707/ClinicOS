# Worker App

Owner: platform/runtime workstream.

Checkpoint 1 will install and wire the outbox processor, Temporal client/workers, retry/dead-letter handling, and workflow tests.

Reserved root command: `npm run dev:worker`.
