{
  "name": "@clinic-os/api",
  "version": "0.0.0",
  "private": true,
  "description": "ClinicOS API. Checkpoint 1 will add the NestJS REST/OpenAPI service.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "node ../../scripts/workspace-command-unavailable.mjs api \"NestJS REST API\" \"The /v1/me contract exists in src/me.ts; register the production NestJS server, OpenAPI generation, auth guards, tenant resolver, and health endpoints before enabling a long-running API dev process.\"",
    "typecheck": "node --check src/index.ts",
    "lint": "node --check src/index.ts",
    "test": "node --test test/*.test.ts",
    "build": "node --check src/index.ts"
  }
}
