{
  "name": "@clinic-os/api",
  "version": "0.0.0",
  "private": true,
  "description": "ClinicOS API. Checkpoint 1 exposes health and session context surfaces.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "npm run build --workspace @clinic-os/config --if-present && node src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "node --check src/index.ts && node --check src/errors.ts && node --check src/health.ts && node --check src/keycloak-verifier.ts && node --check src/local-fixture.ts && node --check src/media-storage.ts && node --check src/me.ts && node --check src/operations.ts && node --check src/server.ts && node --check test/*.test.ts",
    "test": "node --test test/*.test.ts",
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
